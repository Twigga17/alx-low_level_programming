#include "main.h"
/**
 * main - check the code
 * Prints the alphabet in lowercase 
 * Followed by new line
 * Return: 0
 */
void print_alphabet(void)
{
	int c = i;

	for (i = 97; i <= 122; i++)
	_putchar('\n');
	return (0);
}
